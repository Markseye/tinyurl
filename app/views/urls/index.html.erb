<p id="notice"><%= notice %></p>

<h1>Listing Shorts</h1>

<table>
  <thead>
    <tr>
      <th>Long</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @urls.each do |url| %>
      <tr>
        <td><%= url.long %></td>
        <td><%= link_to 'Show', url %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Short', new_url_path %>
