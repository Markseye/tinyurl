<%= form_for(@long) do |f| %>
  <% if @long.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@long.errors.count, "error") %> prohibited this short from being saved:</h2>

      <ul>
      <% @long.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Long URL" %><br>
    <%= f.text_field :long %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
